[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--version" # This is a trick to skip npm install if pnpm is detected

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
