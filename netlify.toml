[build]
  # Force fresh build trigger
  command = "pnpm build"
  publish = "dist"

[build.environment]
  PNPM_FLAGS = "--no-frozen-lockfile"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
